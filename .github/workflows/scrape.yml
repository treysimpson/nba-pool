# Scraper now runs locally via Windows Task Scheduler
# This workflow is intentionally disabled
name: Update Playoff Stats (disabled)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Scraper runs locally. Nothing to do."
